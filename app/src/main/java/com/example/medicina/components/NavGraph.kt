package com.example.medicina.navigation

/*
@Composable
fun AppNavGraph(navController: NavHostController) {
    NavHost(navController = navController, startDestination = "home") {
        composable("home") {
            HomepageScreen()
        }

        composable("search") {
            SearchPage()
        }

        composable("inventory") {
            InventoryPage()
        }

        composable("orders") {
            OrdersPage()
        }
    }
}
*/